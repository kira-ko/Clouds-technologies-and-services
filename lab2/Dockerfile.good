# Хороший Dockerfile с применением best practices
FROM python:3.11-slim

RUN groupadd -r appuser && useradd -r -g appuser appuser

WORKDIR /app

COPY app.py .

RUN chown -R appuser:appuser /app

USER appuser

CMD ["python3", "app.py"]

